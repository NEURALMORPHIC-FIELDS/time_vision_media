# ──────────────────────────────────────
# Time Vision Media — Environment Configuration
# ──────────────────────────────────────
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.

# Application
NODE_ENV=development
PORT=3000
API_URL=http://localhost:3000

# Database (PostgreSQL)
DATABASE_URL=postgresql://timevision:timevision@localhost:5432/timevision

# Cache / Real-time State (Redis)
REDIS_URL=redis://localhost:6379

# Authentication
JWT_SECRET=change-this-to-a-random-secret
SESSION_SECRET=change-this-too

# BaaS — Payment Processing (Swan.io or Mangopay)
BAAS_PROVIDER=swan
BAAS_API_KEY=
BAAS_WEBHOOK_SECRET=

# External APIs
TMDB_API_KEY=your-tmdb-api-key-from-themoviedb.org

# Settlement Configuration
SETTLEMENT_DAY=1
HUB_COST_MARGIN=0.05

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001
